var contador = 1;

// Teste Logico feito no inicio
while (contador <= 10) {
  console.log(`Passo ${contador}`);
  contador++;
}

// Teste Logico feito no final
do {
  console.log(`Passo ${contador}`);
  contador++;
} while (contador <= 10);
